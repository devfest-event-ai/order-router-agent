version: '3.8'

services:
  order-router:
    build: .
    ports:
      - "8000:8000"
    environment:
      - WEBHOOK_SECRET=my-secret-key
      - ALERT_FAILURE_RATE_THRESHOLD=0.2
      - ALERT_LATENCY_THRESHOLD_MS=5000
    volumes:
      - .:/app